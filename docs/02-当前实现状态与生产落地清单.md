# 当前实现状态与生产落地清单

更新日期：2026-08-15。

## 1. 本轮最新成果 (阶段 1 ~ 5 全部打通)

- **真实 SQL Server 物理集成与压测打通**：
  - 在虚拟机（`192.168.134.9`）初始化 `ApprovalDB` 数据库及全量表结构；
  - 编写并执行基于真实 SQL Server 的高并发测试套件，8 并发提批命中 `UX_wf_instance_running_object` 唯一约束，确保单据只能生成 1 个 Running 实例；
  - 验证了 5 分钟 Processing 租约超时崩溃恢复机制。
- **SAP B1 (DB_KCC) 现场元数据与 UDF 扩展**：
  - 实测确认了 `CHORDR`（主表 `@CH_ORDR`，行 `@CH_ORDR_1`，费用 `@CH_ORDR_3`）与 `CHOQUT` 表结构；
  - 补充创建了审批镜像字段 `U_APStatus`、`U_APInstance`、`U_APHash`。
- **数据库级业务门禁 (SBO_SP_TransactionNotification)**：
  - 提供了 GBK 格式的标准 SQL 门禁脚本 `database/03_gatekeeper_transaction_notification.sql`；
  - 在销售订单生成或变更时对来源型号订单状态进行强制拦截阻断。
- **企业统一认证与组织架构映射 (SysUserMapping)**：
  - 在 `ApprovalDB` 中建立了 `sys_user_mapping` 表及 EF 实体映射；
  - 实现了 `UserDirectoryService` 服务，支持按直属主管逐级审批向上追溯（`CandidateType.Manager`）与按岗位角色批量解析（`CandidateType.Role`）；
  - 支持了生效期内的代理人委托（`Delegate`）。
- **审批流高级流转机制**：
  - 扩展实现了任务转交（Forward）接口与不可变轨迹审计；
  - 实现了会签多候选人协同审批。

---

## 2. 数据库脚本资产清单 (全部符合 GBK 编码标准)

1. `database/01_init_approval_db.sql`：ApprovalDB 核心表结构、索引与约束；
2. `database/02_upgrade_reliability.sql`：可靠性升级与租约字段补齐脚本；
3. `database/03_gatekeeper_transaction_notification.sql`：SAP B1 TransactionNotification 业务门禁脚本；
4. `database/04_create_sys_user_mapping.sql`：组织架构与用户统一映射表结构及种子数据。

---

## 3. 测试与质量保障状态

- **单元测试与 Domain 状态机**：6/6 全部通过
- **API 基础与安全集成测试**：4/4 全部通过
- **SQL Server 真实环境并发压测**：3/3 全部通过
- **高级组织树/角色/转交集成测试**：1/1 全部通过
- **前端 Vue 3 TypeScript 编译**：0 Error, 0 Warning
